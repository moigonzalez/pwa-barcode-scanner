import React from 'react';

import styles from './header.css';

const Header = () => (
  <header className="header">
    <h1 className="header__title">PWA Barcode Scanner 🛒</h1>
  </header>
);

export default Header;
